@model List<GameCatalog.Models.Game>

@if (Model.Count == 0)
{
    <h1>Your games are in another castle.</h1>
}

else
{
    foreach (var game in Model)
    {
        <h1>@game.Name</h1>
        <img src="@game.Cover" />
        <p>@game.Description</p>
    }
}
